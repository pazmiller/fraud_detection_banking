# run: pip install -r requirements.txt for installation
google-generativeai>=0.3.0

paddleocr>=2.7.0
paddlepaddle>=2.5.0  # Use paddlepaddle-gpu for GPU support

# CLIP from GitHub (need to manually innstall but requirement will automatically install for you)
git+https://github.com/openai/CLIP.git

torch>=2.0.0
torchvision>=0.15.0
ftfy>=6.1.0
regex>=2023.0.0

opencv-python>=4.8.0
Pillow>=10.0.0
numpy<=1.24.3 
PyMuPDF>=1.23.0  # It's actually: fitz

python-dotenv>=1.0.0
